import CallToAction from "./CTA";

const Hero = () => (
    <div className="hero">
        <p>
            <span>A Physics ready,<br/>Webgl powered,<br/>Javascript Game Engine.</span>
        </p>
        <CallToAction />
    </div>
);

export default Hero;